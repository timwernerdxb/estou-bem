<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-feature android:name="android.hardware.type.watch" />

    <uses-permission android:name="android.permission.BODY_SENSORS" />
    <uses-permission android:name="android.permission.ACTIVITY_RECOGNITION" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.VIBRATE" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_HEALTH" />
    <uses-permission android:name="android.permission.HIGH_SAMPLING_RATE_SENSORS" />

    <application
        android:allowBackup="true"
        android:label="Estou Bem"
        android:supportsRtl="true"
        android:theme="@android:style/Theme.DeviceDefault">

        <meta-data
            android:name="com.google.android.wearable.standalone"
            android:value="true" />

        <!-- Main activity -->
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:theme="@android:style/Theme.DeviceDefault">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Health monitoring foreground service -->
        <service
            android:name=".services.HealthService"
            android:exported="false"
            android:foregroundServiceType="health" />

        <!-- Fall detection foreground service -->
        <service
            android:name=".services.FallDetectionService"
            android:exported="false"
            android:foregroundServiceType="health" />

        <!-- Motion detection foreground service -->
        <service
            android:name=".services.MotionDetectionService"
            android:exported="false"
            android:foregroundServiceType="health" />

        <!-- Wearable listener service — receives messages from phone -->
        <service
            android:name=".services.PhoneConnectionService"
            android:exported="true"
            android:permission="com.google.android.gms.permission.BIND_LISTENER">
            <intent-filter>
                <action android:name="com.google.android.gms.wearable.MESSAGE_RECEIVED" />
                <data
                    android:scheme="wear"
                    android:host="*"
                    android:pathPrefix="/settings" />
                <data
                    android:scheme="wear"
                    android:host="*"
                    android:pathPrefix="/checkin" />
            </intent-filter>
            <intent-filter>
                <action android:name="com.google.android.gms.wearable.DATA_CHANGED" />
                <data
                    android:scheme="wear"
                    android:host="*"
                    android:pathPrefix="/settings" />
                <data
                    android:scheme="wear"
                    android:host="*"
                    android:pathPrefix="/health_data" />
            </intent-filter>
        </service>

    </application>

</manifest>
